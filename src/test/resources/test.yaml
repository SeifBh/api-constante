# Paramétrage pour communiquer avec les autres APIs de la prévoyance
prevoyance:
  api:
    surveillance:
      userKey: JUNIT-TEST
    surveillanceKey: JUNIT-TEST
    userKey: motdepasse
    consumers: TEST_INTEGRATION

# Paramétrage du server
server:
  servlet:
    contextPath: /prevoyance/constante/test

spring:
  postgresql:
    # Paramétrage du Datasource PostgreSQL
    datasource:
      jdbc-url: jdbc:postgresql://fr-in-pazur-x:9411/prevIT
      username: dvptprev
      password: Pr3_v0y4nc3
      platform: postgres
      driver-class-name: org.postgresql.Driver
    # Paramétrage du JPA PostgreSQL
    jpa:
      hibernate:
        schema: test
        hbm2ddl:
          auto: none
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
        showsql: 'true'
      properties:
        hibernate:
          show_sql: 'true'
          temp:
            use_jdbc_metadata_defaults: 'false'
          format_sql: 'true'
          default_schema: prev
          use_sql_comments: 'true'
      database-platform: org.hibernate.dialect.PostgreSQL9Dialect

# Paramétrage pour communiquer avec les autres APIs de la prévoyance
api:
  authent:
    password: TEST_INTEGRATION*motdepasse|SURVEILLANCE*JUNIT-TEST
    constante: TEST_INTEGRATION
  tu: Y

